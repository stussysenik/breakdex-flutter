---
title: Manual Meta & Calibration
name: meta
order: 12
status: stub
verified: null
watches:
  - scripts/docs_ledger_check
  - scripts/docs_bundle
---

# Manual Meta & Calibration

> **Status: stub.** Scope outline below; authored in Phases 4–5.

How the manual stays honest, and the record of what it can be trusted to delegate.

## Will cover

- **The docs ledger:** the `watches` / `verified` / `status` frontmatter contract, and
  `scripts/docs_ledger_check` (git-plumbing drift detection + `--json` work orders).
- **The priming bundle:** `scripts/docs_bundle` (`--all` / `--chapters`, deterministic
  output, TOC + token estimates, standards preamble always included).
- **Priming dry-run record:** the calibration table — for each dry-run, the task, model
  used, bundle subset, token weight, and outcome — so future delegation decisions are
  grounded in evidence, not optimism.

## Calibration log

_(populated by the Phase 5.2 dry-run)_

| Date | Task | Model | Bundle subset | Token weight | Outcome |
| --- | --- | --- | --- | --- | --- |
| — | — | — | — | — | — |
