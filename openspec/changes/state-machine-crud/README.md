# state-machine-crud

Replace imperative dialog-based CRUD with sealed-class state machines across all CRUD surfaces. Fix restored moves notification bug. Model app as closed hierarchical state machine with inline overlays.
