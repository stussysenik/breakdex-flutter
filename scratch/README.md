# scratch/

Debug/repro artifacts and session logs kept out of the repo root. Nothing here is part of
the app, the test suite, or any build — safe to delete entries once their investigation is
closed. Excluded from analysis via `analysis_options.yaml`.
