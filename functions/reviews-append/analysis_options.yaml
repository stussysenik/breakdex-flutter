# Server-side Appwrite Function — pure Dart (no Flutter). Strict language modes
# on; the `context` object at the runtime boundary is inherently dynamic, so the
# app's dynamic-call lints are intentionally not inherited here.
analyzer:
  language:
    strict-casts: true
    strict-inference: true
    strict-raw-types: true
